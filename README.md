# EventMatch Agent — OpenAI + NVIDIA

Текущая версия расширяет приложенный baseline: диалог с сохранением заказа,
строгий подбор, проверка источников объяснений и подготовка NVIDIA embeddings.

**Запуск:** Python 3.11+, без pip-зависимостей.

```bash
cp .env.example .env
python -m eventmatch serve
```

Windows: `Copy-Item .env.example .env`. Откройте http://127.0.0.1:8000.
Ключи указываются только в локальном `.env` или переменных окружения.
Для NVIDIA выполните `python -m tools.prepare_semantics`, установите
`EVENTMATCH_RANKER=nvidia`, перезапустите сервер.
Без подготовленного артефакта базовый режим честно обозначается как baseline.

[Актуальные инструкции, API и ограничения](docs/AGENT_API.md).
Поставляемый код содержит интеграции; успешные live-вызовы провайдеров
и подготовленный NVIDIA-артефакт не входят в эту поставку.

---

## Архивное описание baseline v0.1

Далее сохранена документация исходного архива. Утверждения об отсутствии
LLM относятся только к baseline; текущую архитектуру см. выше.

# EventMatch · #79-lite

**До трёх event-подрядчиков с проверяемым объяснением выбора.**

Решение хакатон-трека «Умный подбор подрядчиков» для каталога Казахстана. Главная ценность — помочь сравнить допустимых кандидатов, объяснить различия и честно показать ограничения каталога.

В репозитории есть **подробный план решения и работающий reference MVP**: 66 исходных профилей, локальный API, простой интерфейс, детерминированный подбор, источники объяснений, семь демозапросов и 22 автоматических теста.

## Запуск за минуту

Нужен **Python 3.11+**. Команды выполняются из папки `eventmatch-79-lite`. Внешние Python-зависимости, API-ключи и интернет для запуска не нужны.

```console
python -m eventmatch serve
```

Открыть **http://127.0.0.1:8000**. Кнопки над формой заполняют и выполняют реальные демозапросы. Остановка — `Ctrl+C`; другой порт — `python -m eventmatch serve --port 8080`.

Проверить без браузера:

```console
python -X utf8 -m tools.demo
python -X utf8 -m eventmatch recommend --request examples/01-dense.json
python -X utf8 -m unittest discover -s tests -v
python -X utf8 -m tools.check_repository
```

`-X utf8` удобен в Windows PowerShell для корректной кириллицы. Дата ограничена календарём **2026-09-23 — 2026-12-31**; для воспроизводимости демонстрация не зависит от сегодняшнего дня.

## С чего читать

| Документ | Содержание |
|---|---|
| [Стратегия решения](docs/SOLUTION.md) | Что действительно оценивают, продуктовая логика, оптимальный подход к 66 профилям |
| [Подробный план реализации](docs/IMPLEMENTATION_PLAN.md) | Этапы на 24 часа, задачи, зависимости, критерии приёмки, приоритеты |
| [Анализ данных](docs/DATA_AUDIT.md) | Реальные распределения, календарь, противоречия, выводы для алгоритма |
| [Архитектура](docs/ARCHITECTURE.md) | Пайплайн, правила, ранжирование, объяснения, план AI-слоя |
| [Контракт API](docs/API.md) | Вход, три исхода, диагностика, ошибки, примеры вызова |
| [Качество и проверки](docs/EVALUATION.md) | Definition of Done, автоматические и ручные критерии, метрики |
| [Демонстрация и защита](docs/DEMO.md) | Реальные запросы и ожидаемые карточки, тайминг показа, ответы жюри |
| [Решения по архитектуре](docs/DECISIONS.md) | Почему такие технологии, где оправдан AI и что развивать дальше |

## Как устроен подбор

```text
Валидация запроса
  → город + точная категория: исходный круг кандидатов
  → календарь + бюджет + формат + язык + часы: жёсткие ограничения
  → устойчивое ранжирование допустимых
  → до 3 карточек: факты запроса + отличающая цитата из профиля
  → понятная сводка исключений + источники + версия данных
```

**Три явных исхода:**

- `matched` — есть от одного до трёх вариантов; объясняем, если их меньше трёх.
- `no_category_in_city` — в исходном каталоге города нет такой категории.
- `no_match` — категория в городе есть, но все кандидаты нарушают хотя бы одно условие.

Неверный запрос, например дата вне известного календаря, — отдельная ошибка валидации, HTTP 422.

### Что уже реализовано

- Загрузка и проверка типизированного JSONL, преобразованного из предоставленного CSV.
- Все обязательные входы, опциональные длительность и язык.
- Один механизм для людей, коллективов и площадок.
- Строгая проверка занятости, включая профили с `max_hours = null`.
- Устойчивый порядок: лексическая релевантность описания → стартовая цена → `id`.
- Два предложения объяснения с точной цитатой и координатами в исходном описании.
- Отдельные счётчики основных и всех причин исключения; причинная диагностика даты.
- Проверенные подсказки для пустой выдачи: ближайшая дата в пределах ±7 дней или минимальный достаточный бюджет, если достаточно изменить только его.
- Видимые метки синтетики и проставленных значений.
- Локальный HTTP API, форма, CLI, CI-конфигурация, тесты и снимки ответов.

### Что запланировано для усиления

AI-слой пока **не реализован**. Текущий алгоритм — объяснимый rule-based/лексический baseline. Он не выдаётся за LLM, эмбеддинги или обученную модель.

Главное развитие: офлайн извлечь из описаний признаки с цитатами-основаниями, проверить источники, заморозить артефакт и использовать готовые эмбеддинги для выбора наиболее полезного объяснения. Сравнить с baseline на размеченных запросах. Конкретная спецификация — в [архитектуре](docs/ARCHITECTURE.md#7-целевой-ai-слой-план).

## Проверенные демосценарии

| Запрос | Фактический результат |
|---|---|
| Алматы, ведущий, корпоратив, **12.10**, 1,3 млн ₸, 6 ч, русский | 7 допустимых → Куррапика, Буллма, Джинбей |
| Те же условия, **13.10** | 7 допустимых → Куррапика, Сон Гоку, Буллма; Сон Гоку занят 12.10 |
| Алматы, флорист, свадьба, **12.10**, 300 тыс. ₸, 8 ч, русский | 2 из 2 → Тихиро Огино и Тони Тони Чоппер |
| Алматы, ведущий, корпоратив, **12.12**, 1,3 млн ₸, 6 ч, английский | 0; все 3 кандидата, проходящие остальные условия, заняты |
| Астана, декоратор, **12.10** | Категории нет в этом городе |
| Алматы, банкетный зал, свадьба, **14.11**, 4 млн ₸, 8 ч, русский | 2 → Иноскэ Хашибира и Гохан |
| Алматы, ведущий, корпоратив, **12.10**, 100 тыс. ₸ | 0; изменение только бюджета до 500 тыс. ₸ даст один вариант |

Полные входы — `examples/*.json`, ответы — `examples/responses/*.json`. `tools.demo` каждый раз считает выдачу заново, а не проигрывает сохранённые ответы.

## Данные и воспроизводимость

- 66 профилей, 17 категорий; Алматы — 50, Астана — 15, Зарубежье — 1.
- 53 профиля с `synthetic=false`, 13 синтетических профилей организаторов.
- Команда добавила **0** профилей. Поля и календари исходного CSV сохранены.
- Исходный JSONL-файл организаторов не был предоставлен в рабочей папке; включённый JSONL воспроизводимо получен из их CSV.
- Происхождение, преобразования и SHA-256 — [data/manifest.json](data/manifest.json).
- Цена «от» — нижняя граница за мероприятие; не расчёт окончательной сметы.
- `max_hours=null` — ограничение неприменимо, а не неизвестная или бесконечная длительность присутствия.

Пересчитать аудит и измерить скорость:

```console
python -X utf8 -m tools.audit
python -X utf8 -m tools.benchmark
```

Для повторного импорта из исходного CSV:

```console
python -X utf8 -m tools.import_dataset "C:\path\hackathon dataset anonymized .csv"
python -X utf8 -m tools.audit
python -X utf8 -m tools.demo --write
```

Импорт перезаписывает канонический JSONL и манифест. Обычный запуск использует включённые данные и ничего скачивать не требует.

## Структура

```text
eventmatch-79-lite/
├── README.md
├── pyproject.toml               # Python 3.11+, runtime dependencies: []
├── docs/                        # Аналитика, архитектура, подробный план, защита
├── data/
│   ├── manifest.json            # Происхождение и контрольные суммы
│   ├── raw/*.jsonl              # 66 типизированных профилей из CSV
│   └── reports/                 # Воспроизводимый аудит и локальный benchmark
├── eventmatch/
│   ├── models.py                # Схемы и валидация
│   ├── catalog.py               # Загрузка и версия каталога
│   ├── engine.py                # Ограничения, ранжирование, evidence, диагностика
│   ├── server.py                # HTTP-адаптер локального демо
│   └── __main__.py              # CLI и запуск сервера
├── web/index.html               # Одна форма, карточки, причины, метки данных
├── examples/                    # Запросы и точные снимки ответов
├── tests/                       # 22 теста бизнес-правил, детерминизма и HTTP
├── tools/                       # Импорт, аудит, live demo, benchmark
└── .github/workflows/ci.yml      # Проверки на Python 3.11 / 3.12
```

## Текущий результат проверки

Локально на Python 3.12.8 / Windows прошли **22 теста** и все семь демонстрационных запросов. Включая независимую проверку ограничений на всех 100 датах для контрольного запроса и сравнение после перезапуска с разными `PYTHONHASHSEED`.

Сохранённый замер: **350 вызовов ядра**, p50 **0,403 мс**, p95 **1,583 мс**, максимум **5,029 мс**. Это локальное время прогретого ядра, без HTTP, браузера и параллельной нагрузки; полный результат — [benchmark.json](data/reports/benchmark.json). Это не обещание production SLA.

HTTP-интеграция проверена тестами; визуальная проверка в браузере и слепая оценка полезности объяснений входят в предзащитный план. CI-файл подготовлен, удалённый запуск GitHub Actions здесь не выполнялся.

## Границы текущей версии

Лексические признаки и извлечение предложений не понимают отрицания и сложные смысловые связи. Источник цитаты доказывает наличие утверждения **в профиле**, а не независимую достоверность рекламы. У нас нет рейтингов качества, статистики успешных заказов, окончательных цен и сведений о свободных датах за пределами календаря.

Локальный сервер на стандартной библиотеке предназначен для живой демонстрации. Для публичного пилота план предусматривает FastAPI, формальный OpenAPI-контракт, наблюдаемость и версионирование данных.
